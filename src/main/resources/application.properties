# JDBC driver class used by Java to connect to MySQL
db.driver=com.mysql.cj.jdbc.Driver

# EC2 host MySQL connection (from inside Docker)
db.host=jdbc:mysql://host.docker.internal:3306

# Database name you created
db.name=onlinebookstore

# MySQL user credentials
db.username=root
db.password=Project@123
